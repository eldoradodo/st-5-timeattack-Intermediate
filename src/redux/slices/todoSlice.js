// TODO: todoSlice 를 작성하세요.

import { createSlice } from '@reduxjs/toolkit'

const counterSlice = createSlice({
    name: 'todos',
    initialState,
    reducers: {},
    
    deleteTodo: () => {},
    complateTodo: () => {},
    cancleTodo: () => {},